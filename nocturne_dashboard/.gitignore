node_modules/
.next/
out/
build/
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Snapshot exports pulled from Snowflake — may contain page titles and URLs.
/public/snapshots/

# The Snowflake Node driver writes this on every connection. It records the
# account identifier, the signed-in user, the role and the warehouse in
# cleartext, so it must never be committed.
snowflake.log

# Firebase CLI deploy cache — local state, not source.
.firebase/
