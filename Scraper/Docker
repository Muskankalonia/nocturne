FROM python:3.11-slim

RUN apt-get update && apt-get install -y tor && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir PySocks requests beautifulsoup4 stem

COPY scraper.py /app/scraper.py
WORKDIR /app

CMD bash -c "tor & sleep 20 && python -u scraper.py"